import { CategoryItemType, CATEGORY_KEYS } from "../constants/categories"

export type CategoryItemProps = {
  // icon: any,
  // hoverIcon?: any,
  // text: string,
  // categoryKey: CATEGORY_KEYS
  category: CategoryItemType
}
